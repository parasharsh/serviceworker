This project is based on https://github.com/ganatan/angular-example-starter.

Build the Production package using ng build --prod

npm install -g http-server

cd dist and execute below command

http-server -c-1 . 
 
